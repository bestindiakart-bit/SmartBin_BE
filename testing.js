let arr = [1, 2, 1, 3, 4, 2, 3];

function distinct(arr) {
  let char = {};
  for (let i = 0; i < k; i++) {
    if (!char[arr[i]]) {
      char[arr[i]] = (char[arr[i]] || 0) + 1;
    }
  }

  for (let key of char) {
    if (key == 1) {
      count++;
    }
  }

  for (let i = k; i < arr.length; i++) {}

  for (let i = k; i < arr.length; i++) {}
}
